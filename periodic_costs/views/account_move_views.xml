<odoo>
    <record id="view_invoice_tree" model="ir.ui.view">
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_invoice_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='amount_total_signed']" position="after">
                <field name="cost_warning" invisible="1"/>
                <field name="cost_tag_ids" widget="many2many_tags" options="{'color_field': 'color'}"/>
                <field name="cost_date_start"/>
                <field name="cost_date_end"/>
            </xpath>

            <xpath expr="//tree" position="attributes">
                <attribute name="decoration-danger">cost_warning != False</attribute>
            </xpath>

        </field>
    </record>

    <record id="view_move_form" model="ir.ui.view">
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <xpath expr="//header" position="after">
                <div class="alert alert-danger mt0 mb0 text-center" role="alert" attrs="{'invisible' : [('cost_warning', '=' ,False)]}">
                    <field name="cost_warning" readonly="1"/>
                </div>
            </xpath>
            <field name="journal_id" position="before">
                <label for="cost_date_start" string="Cost Range" attrs="{'invisible': [('type', 'not in', ('out_invoice', 'out_refund', 'in_invoice', 'in_refund', 'out_receipt', 'in_receipt'))]}"/>
                <div class="d-flex" attrs="{'invisible': [('type', 'not in', ('out_invoice', 'out_refund', 'in_invoice', 'in_refund', 'out_receipt', 'in_receipt'))]}">
                    <field name="cost_date_start" placeholder="From"/>
                    <span class="o_form_label mx-3"> &#8594; </span>
                    <field name="cost_date_end"  placeholder="To"/>
                </div>
            </field>
            <field name="invoice_vendor_bill_id" position="before">
                <field name="cost_tag_ids" widget="many2many_tags" options="{'color_field': 'color'}"/>
            </field>
        </field>
    </record>
</odoo>